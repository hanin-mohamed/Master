//import javax.swing.JOptionPane;
import javax.swing.plaf.IconUIResource;
import javax.xml.crypto.dsig.spec.XSLTTransformParameterSpec;
import java.util.Scanner;
import java.util.ArrayList;
import static java.lang.Math.*;
import java.lang.*;
import java.util.*;
public class Array {
    public static void main(String[] args) {
     /*   String []arr=new String[] {"haneen","mohamed","salama"};
        for (String i:arr)
        System.out.println(i);*/
        Scanner input = new Scanner(System.in);
       //
        // int[] arr = {2, 5, 7, 10, 10};
      //  int size=input.nextInt();
       /* int arr[]=new int[size];
        double sum = 0;
        boolean flag=false;
        //int num=input.nextInt();
        for (int i = 0; i < size; i++) {
            arr[i] = input.nextInt();
        }*/
           // sum += arr[i];
           /* if (arr[i]==num){
                flag=true;
                break;
            }*/
            /*
            ********** min,max
        int min=arr[0],max=arr[0];
        for (int i = 0; i < arr.length; i++) {
            if (arr[i]>max)
                max=arr[i];
            if (arr[i]<min)
                min=arr[i];
        }
        System.out.println("minNum = "+min+"  && maxNum = "+max);*/

        /*
        ********sort array
        Arrays.sort(arr);
            System.out.print("sortedArray: ");
            for(int k: arr)
                System.out.print(k+" ");
*/

       /* if (flag==true)
            System.out.println("Founded!");
        else
            System.out.println("Not founded!");*/

       // double avg=sum/arr.length;
       // System.out.println("average = "+avg);

        /*char array
         */
       /* char arrC[]=new char[4];
        arrC[0]=99;
        arrC[1]=98;
        arrC[2]=100;
        arrC[3]=99;
        System.out.println(arrC);*/
        //char arrC[]=new char[5];
       // Scanner input=new Scanner(System.in);
       // arrC=input.nextLine().toCharArray();  //nextLine to read spaces in string
        //==String str= input.nextLine();  arrC=str.toCharArray();
       // System.out.println(arrC);
        /* sort array&&print as string:
        char arr[]=new char[]{'h','A','t','i','q','a','R','Y'};
        Arrays.sort(arr);
        System.out.println(arr);
        String str=new String(arr,1,4);   string str ="absg";
        System.out.println(str);*/
        //anonymous array --> fun(new int[]{1,2,3});
        //getArrat();==>index;
    }
}
